name: CI

on:
  push:
  pull_request:

jobs:
  build-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install backend deps
        run: |
          cd backend
          npm ci
      - name: Run backend lint/test (if any)
        run: |
          cd backend
          # placeholder for tests: npm test
          echo "No tests configured"
